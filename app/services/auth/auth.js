var User     = require('../database/schemas/User');

//done is a callback with (user,error) parameters
exports.authLocal = function(done)
{
  //find and return the user
}

exports.registerLocal = function(done)
{
  
}